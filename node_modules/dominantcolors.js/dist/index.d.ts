interface IOptions {
    count: number;
    colorFormat: string;
    method: string;
    omitTransparentPixel: boolean;
}
export default function (image: string | Uint8ClampedArray | number[], options?: IOptions): Promise<(string | Object)[]>;
export {};
