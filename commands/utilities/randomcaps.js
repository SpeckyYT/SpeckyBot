const { round, random } = require("mathjs");

module.exports.run = async (bot, msg, args, config) => {
   
}

module.exports.config = {
    name: "randomcaps",
    description: "Turns a sentence into random caps!",
    usage: `<text>`,
    category: `utilitiess`,
    accessableby: "Members",
    aliases: ["rcaps"]
}
